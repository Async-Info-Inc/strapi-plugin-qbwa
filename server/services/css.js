"use strict";

module.exports = () => {
    return {
        init: async () => {},
        readFile: async (name) => {},
        writeFile: async (name, data) => {},
        listFiles: async () => {},
        deleteFile: async (name) => {},
    };
};
